package api;

import (
	"net/http"
)

// postPhoto handles the POST /photo API endpoint.
func (rt *_router) postPhoto(w http.ResponseWriter, r *http.Request, ps httprouter.Params) {
	
}

// getPhoto handles the GET /photo/:id API endpoint.
func (rt *_router) getPhoto(w http.ResponseWriter, r *http.Request, ps httprouter.Params) {
}

// deletePhoto handles the DELETE /photo/:id API endpoint.
func (rt *_router) deletePhoto(w http.ResponseWriter, r *http.Request, ps httprouter.Params) {
}
